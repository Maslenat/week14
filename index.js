function calcSum(){
    let a = prompt('Введите первый аргумент', );
    let b = prompt('Введите второй аргумент', );
    alert (Number(a)+Number(b));

   
}
function calcSub(){
    let a = prompt('Введите первый аргумент', );
    let b = prompt('Введите второй аргумент', );
    alert (Number(a)-Number(b));

   
}
function calcMult(){
    let a = prompt('Введите первый аргумент', );
    let b = prompt('Введите второй аргумент', );
    alert (Number(a)*Number(b));

   
}
function calcDiv(){
    let a = prompt('Введите первый аргумент', );
    let b = prompt('Введите второй аргумент', );
    alert (Number(a)/Number(b));

   
}
let showMessage=() =>{let name = prompt('Как тебя зовут?', );
alert(`Привет, ${name}!`); 


};

